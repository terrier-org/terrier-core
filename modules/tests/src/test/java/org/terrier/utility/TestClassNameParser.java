package org.terrier.utility;

import static org.junit.Assert.*;

import java.util.List;

import org.junit.Test;
//import org.terrier.querying.prediction.RetrievalPredictor;

public class TestClassNameParser {

	// List<RetrievalPredictor> get(String names) {
	// 	List<RetrievalPredictor> rtr = null;
	// 	try{
	// 		rtr = new ClassNameParser<RetrievalPredictor>(
	// 				names, RetrievalPredictor.class.getPackage().getName(), RetrievalPredictor.class)
	// 				.parseAll();
	// 	}catch (Exception e) {
	// 		throw new RuntimeException(e);
	// 	}
	// 	return rtr;
	// }
	
	// @Test
	// public void test() {
	// 	assertEquals(1, get("QueryScope").size());
	// 	assertEquals(1, get("org.terrier.querying.prediction.QueryScope").size());
	// 	assertEquals(2, get("QueryScope,QueryDifficulty").size());
	// 	assertEquals(1, get("ClarityScore(20)").size());		
	// }

}